function HandleOnRequestFinished(AIRequestID, Result) {
  // Handle the completion of the AI request
  // For example, update the UI with the result, or process the data further
}

// Example usage
let requestID = 12345;
let requestData = /* ... */;
sendAIRequest(requestID, requestData, HandleOnRequestFinished);
